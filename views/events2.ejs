<% layout('layout/boiler')  %> 
<% let jl=arr.length %> 
<% let ceilno=Math.ceil(jl/3) %> 

<% arr %> 

<div class="top">

</div>

<% let p=arr.length %> 
<% p  %> 
<% let d=Math.ceil(p/3) %> 
<% var count=0 %> 
<% let b = 0; %>
<section class="bgw rooms rooms-widget rooms-inner" background-color="white">

            <!-- === rooms header === -->
           
            

            <!-- === rooms content === -->

            <div class="bgw container">
                <% for(let i=1;i<=d;i++){ %>
                <div class="bgw owl-rooms owl-theme">

                    <!-- === rooms item === -->
                    <% for(let j=1;j<=3;j++){ %> 
                        <% count=count+1 %> 
                        <% if(count<=p){ %> 
                    <div class="bgw item">
                        <article>
                            <div class="bgw image">
                                <img src="<%=arr[b].img %> " style="width:100%;height:250px;" alt="" />
                            </div>
                            <div class="details">
                                <div class="text">
                                    <h3 class="title"><a href="/events2/<%=arr[b]._id%> "><%=arr[b].title  %> </a></h3>
                                    <p>Single room</p>
                                </div>
                                <div class="book">
                                    <div>
                                        <a href="/events2/<%=arr[b]._id  %> " class="btn btn-main">View >></a>
                                    </div>
                                    <div>
                                        <span class="price h4">&#8377;  <%=arr[b].price  %> </span>
                                        <span></span>
                                    </div>
                                </div>
                            </div>
                        </article>
                        <%  b=b+1 %>
                    </div>
                    <% } else{ %>
                        <!-- <div class=".mb">

                        </div> -->
                        <% } %> 
                    <% } %> 

                    
                </div><!--/owl-rooms-->
                <% } %> 
            </div> <!--/container-->

        </section>

<style>
    .cat{
    position:fixed;
    top: 0;
    left: 0;
    background-color: rebeccapurple;
    height: 100%;
    width: 20%;
    
}
    .top{
        margin-top: 310px;
    }
    .bgw{
     background-color: #eeeeee;
    }
    body{
        background-color: #eeeeee;
    }
    @media only screen and (max-width: 800px) {
  .top{
      margin-top: 40px;
  }
      
  }
</style>
